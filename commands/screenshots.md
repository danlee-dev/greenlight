---
description: Generate spec-perfect store screenshots from the app's real screens
argument-hint: [ios|android|both]
---
Use the screenshot-studio skill and the Greenlight MCP server to generate store screenshots for $ARGUMENTS.

1. Capture REAL app screens (iOS Simulator / Android emulator or user-provided captures). Never invent UI that is not in the build (Apple 2.3.3).
2. Compose them into device-framed marketing screenshots at exact 2026 spec sizes.
3. Localize headline copy per target locale (default en + ko).
4. Render PNGs into ./renders and open the cockpit for live tweaking.
